{
  "name": "Organization",
  "type": "object",
  "properties": {
    "nom": {
      "type": "string",
      "description": "Nom de l'organisation / agence"
    },
    "description": {
      "type": "string",
      "description": "Description de l'organisation"
    },
    "membres": {
      "type": "string",
      "description": "Liste des emails des membres s\u00e9par\u00e9s par des virgules"
    },
    "invite_code": {
      "type": "string",
      "description": "Code d'invitation au groupe (g\u00e9n\u00e9r\u00e9 par le chef, valable 30 min)"
    },
    "invite_code_expires": {
      "type": "string",
      "format": "date-time",
      "description": "Date/heure d'expiration du code d'invitation"
    }
  },
  "required": [
    "nom"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "$or": [
        {
          "id": "{{user.data.organization_id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}